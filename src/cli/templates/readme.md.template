# {{server_name}} MCP Server

{{server_description}}

## Tools

This MCP server provides the following tools:

{{tools_list}}

## Installation

```bash
pip install -e .
```

## Usage

Start the MCP server:

```bash
python -m {{server_package}}.{{server_package}}_server
```

Or use the FastAPI app in your own code:

```python
from {{server_package}} import create_app

app = create_app()
```
